# Copy these into Render Environment Variables instead of committing a real .env
# Rotate any secrets that were previously exposed in VCS.

# Google Gemini API
GEMINI_API_KEY=

# WhatsApp Cloud API webhook verify token (arbitrary string you set in Meta dashboard)
WHATSAPP_VERIFY_TOKEN=

# Google OAuth (for Calendar/Meet)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
